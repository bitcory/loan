---
phase: 02-audit-logging
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/audit.ts
autonomous: true
requirements: [AUDIT-02, AUDIT-04]
must_haves:
  truths:
    - "logAudit()를 호출하면 audit_logs 테이블에 레코드가 생성된다"
    - "logAudit() 내부에서 에러가 발생해도 호출 측에 예외가 전파되지 않는다 (fire-and-forget)"
    - "residentNumber/businessNumber 필드가 포함된 데이터를 maskPii()에 넘기면 '***MASKED***'로 치환된다"
    - "getClientIp()가 x-forwarded-for 첫 번째 값 또는 x-real-ip를 반환한다"
    - "sanitizeForLog()가 Decimal 타입을 number로, Date를 ISO 문자열로 변환한다"
  artifacts:
    - path: "src/lib/audit.ts"
      provides: "logAudit, maskPii, sanitizeForLog, getClientIp 함수"
      exports: ["logAudit", "maskPii", "sanitizeForLog", "getClientIp", "AuditAction", "AuditContext"]
  key_links:
    - from: "src/lib/audit.ts"
      to: "prisma.auditLog.create"
      via: "basePrisma import as prisma"
      pattern: "prisma\\.auditLog\\.create"
    - from: "src/lib/audit.ts"
      to: "next/headers"
      via: "headers() in getClientIp"
      pattern: "headers\\(\\)"
---

<objective>
`src/lib/audit.ts` 유틸리티 모듈을 새로 생성한다. 이 모듈은 모든 액션 파일이 감사 로그를 기록할 때 사용하는 공용 인터페이스다.

Purpose: Plan 03에서 15개 뮤테이션에 logAudit() 호출을 추가하기 위한 공용 헬퍼를 제공한다. PII 마스킹(AUDIT-04)과 메타데이터 캡처(AUDIT-02)를 한 곳에서 처리한다.
Output: src/lib/audit.ts (신규 파일, ~80줄)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-audit-logging/02-01-SUMMARY.md
@src/lib/prisma.ts

<interfaces>
<!-- src/lib/prisma.ts 핵심 export -->
```typescript
// basePrisma는 'prisma'라는 이름으로 export된다
export { basePrisma as prisma };

// getTenantClient는 organizationId 필터가 적용된 확장 클라이언트를 반환
// AuditLog는 getTenantClient extension 대상이 아님 — basePrisma(prisma)를 직접 사용해야 함
export function getTenantClient(organizationId: string): ExtendedPrismaClient;
```

<!-- next/headers 사용 패턴 (Next.js 14 App Router) -->
```typescript
import { headers } from "next/headers";
// headers()는 서버 액션/Route Handler 내부에서만 호출 가능
// 모듈 스코프에서 호출하면 "headers was called outside a request scope" 에러 발생
// try/catch로 감싸서 컨텍스트 외부 호출 시 undefined 반환
```

<!-- Prisma Decimal 타입 직렬화 주의사항 -->
// Decimal.toNumber() 로 변환해야 JSON.stringify 가능
// instanceof Date → .toISOString() 변환 필요
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/audit.ts 생성 — maskPii, sanitizeForLog, getClientIp, logAudit</name>
  <files>src/lib/audit.ts</files>
  <behavior>
    - maskPii({ residentNumber: "900101-1234567" }) → { residentNumber: "***MASKED***" }
    - maskPii({ businessNumber: "123-45-67890" }) → { businessNumber: "***MASKED***" }
    - maskPii({ name: "홍길동", residentNumber: "9001011234567" }) → { name: "홍길동", residentNumber: "***MASKED***" }
    - maskPii({ nested: { residentNumber: "900101-1234567" } }) → { nested: { residentNumber: "***MASKED***" } }
    - maskPii({ loanAmount: 50000000 }) → { loanAmount: 50000000 } (PII 아닌 필드는 그대로)
    - sanitizeForLog이 Decimal 타입 객체를 number로 변환한다
    - sanitizeForLog이 Date를 ISO 문자열로 변환한다
    - sanitizeForLog이 passwordHash 필드를 제거한다
    - logAudit 내부에서 prisma.auditLog.create가 에러를 throw해도 logAudit 자체는 throw하지 않는다
  </behavior>
  <action>
아래 전체 내용으로 src/lib/audit.ts를 생성한다:

```typescript
import { headers } from "next/headers";
import { prisma } from "@/lib/prisma";

export type AuditAction = "CREATE" | "UPDATE" | "DELETE";

export interface AuditContext {
  userId: string;
  organizationId: string;
  ipAddress?: string;
}

// PII 마스킹 대상 필드명 whitelist
const PII_FIELDS = new Set(["residentNumber", "businessNumber"]);

// 민감정보 필드를 재귀적으로 마스킹한다.
// Date 객체는 재귀 대상에서 제외한다 (typeof "object"이지만 entries()가 없음).
export function maskPii(data: Record<string, unknown>): Record<string, unknown> {
  const result: Record<string, unknown> = {};
  for (const [key, value] of Object.entries(data)) {
    if (PII_FIELDS.has(key) && value) {
      result[key] = "***MASKED***";
    } else if (
      value !== null &&
      typeof value === "object" &&
      !Array.isArray(value) &&
      !(value instanceof Date)
    ) {
      result[key] = maskPii(value as Record<string, unknown>);
    } else {
      result[key] = value;
    }
  }
  return result;
}

// DB 레코드를 감사 로그 스냅샷으로 변환한다.
// - passwordHash, organizationId, updatedAt 제거
// - Prisma Decimal → number 변환 (JSON.stringify 대응)
// - Date → ISO 문자열 변환
// - PII 마스킹 적용
export function sanitizeForLog(record: Record<string, unknown>): Record<string, unknown> {
  const EXCLUDED = new Set(["passwordHash", "organizationId", "updatedAt"]);
  const out: Record<string, unknown> = {};
  for (const [k, v] of Object.entries(record)) {
    if (EXCLUDED.has(k)) continue;
    if (v !== null && typeof (v as { toNumber?: unknown }).toNumber === "function") {
      out[k] = (v as { toNumber(): number }).toNumber();
    } else if (v instanceof Date) {
      out[k] = v.toISOString();
    } else {
      out[k] = v;
    }
  }
  return maskPii(out);
}

// 요청 헤더에서 클라이언트 IP를 추출한다.
// x-forwarded-for는 프록시 체인을 거치면 콤마 구분 목록이 될 수 있으므로 첫 번째만 사용.
// 서버 액션 컨텍스트 외부에서 호출되면 undefined를 반환한다 (에러 전파 안 함).
export function getClientIp(): string | undefined {
  try {
    const h = headers();
    return (
      h.get("x-forwarded-for")?.split(",")[0]?.trim() ||
      h.get("x-real-ip") ||
      undefined
    );
  } catch {
    return undefined;
  }
}

// 감사 로그를 audit_logs 테이블에 기록한다.
// 중요: basePrisma(prisma)를 사용한다 — 테넌트 extension(ctx.db)은 AuditLog를 포함하지 않는다.
// 중요: 내부 에러는 console.error로 출력하되 절대 throw하지 않는다.
//        감사 로그 실패가 비즈니스 로직을 중단해서는 안 된다.
// 중요: logAudit은 뮤테이션 DB write가 성공한 후 호출되어야 한다.
//        뮤테이션 트랜잭션 내부에서 호출하지 말 것 (롤백 시 로그도 사라짐).
export async function logAudit(
  ctx: AuditContext,
  entityType: string,
  entityId: string,
  action: AuditAction,
  before: Record<string, unknown> | null,
  after: Record<string, unknown> | null,
): Promise<void> {
  try {
    await prisma.auditLog.create({
      data: {
        organizationId: ctx.organizationId,
        userId: ctx.userId,
        entityType,
        entityId,
        action,
        before: before ? maskPii(before) : undefined,
        after: after ? maskPii(after) : undefined,
        ipAddress: ctx.ipAddress,
      },
    });
  } catch (err) {
    // 감사 로그 실패는 콘솔에만 기록 — 호출 측에 예외를 전파하지 않는다
    console.error("[audit] 감사 로그 기록 실패:", err);
  }
}
```

주의사항:
- `import { prisma } from "@/lib/prisma"` — basePrisma를 가져온다. getTenantClient()가 아님.
- `getClientIp()`는 항상 try/catch로 감싼다 — 서버 액션 컨텍스트 외부에서 안전하게 호출 가능
- `logAudit`의 try/catch는 제거하지 않는다 — 감사 실패가 사용자 작업을 막으면 안 된다
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- src/lib/audit.ts 파일이 존재한다
- `npx tsc --noEmit`이 audit.ts 관련 타입 에러를 보고하지 않는다
- maskPii, sanitizeForLog, getClientIp, logAudit, AuditAction, AuditContext가 export되어 있다
- maskPii({ residentNumber: "900101-1234567" })이 { residentNumber: "***MASKED***" }를 반환한다
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript 컴파일 에러 없음
npx tsc --noEmit

# audit.ts가 올바른 export를 가지고 있는지 확인
node -e "
const audit = require('./src/lib/audit');
// maskPii 동작 검증
const masked = audit.maskPii({ residentNumber: '900101-1234567', name: '홍길동' });
console.assert(masked.residentNumber === '***MASKED***', 'PII 마스킹 실패');
console.assert(masked.name === '홍길동', '일반 필드 변경됨');
console.log('maskPii 검증 통과');
"
```
</verification>

<success_criteria>
- src/lib/audit.ts 파일이 생성되어 있다
- TypeScript 컴파일이 에러 없이 완료된다
- maskPii()가 residentNumber와 businessNumber를 "***MASKED***"로 치환한다
- sanitizeForLog()가 Decimal을 number로, Date를 ISO 문자열로 변환한다
- logAudit()가 에러 발생 시 throw하지 않고 console.error만 출력한다
- getClientIp()가 try/catch로 감싸져 있어 컨텍스트 외부에서도 안전하다
</success_criteria>

<output>
완료 후 `.planning/phases/02-audit-logging/02-02-SUMMARY.md`를 생성한다.

포함할 내용:
- export된 함수/타입 목록
- PII_FIELDS Set에 포함된 필드명
- logAudit의 에러 처리 전략 (fire-and-forget)
- prisma import 경로 (`@/lib/prisma` — basePrisma)
</output>
