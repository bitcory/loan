---
phase: 01-auth-multi-tenant-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - prisma/seed.ts
  - prisma/migrations/backfill-org/run.ts
autonomous: true
requirements:
  - TENANT-04
  - TENANT-05

must_haves:
  truths:
    - "기본 조직(Default Organization)이 DB에 생성된다"
    - "기존 7개 테이블의 모든 row에 organizationId가 채워진다 (null 없음)"
    - "기본 조직의 ADMIN 사용자 계정이 생성된다"
    - "기본 설정값(maxLtvRate, defaultInterestRate, legalMaxRate, overdueRate)이 seed로 생성된다"
  artifacts:
    - path: "prisma/migrations/backfill-org/run.ts"
      provides: "backfill 스크립트"
      contains: "DEFAULT_ORG_ID"
    - path: "prisma/seed.ts"
      provides: "기본 조직 + ADMIN 계정 + 기본 Setting rows seed"
      contains: "Default Organization"
  key_links:
    - from: "prisma/migrations/backfill-org/run.ts"
      to: "prisma DB"
      via: "prisma.$executeRaw or updateMany"
      pattern: "organizationId.*null"
---

<objective>
기본 조직(Default Organization) seed를 실행하고, 기존 7개 테이블의 모든 row에 backfill을 적용한다 (3-step migration의 2단계). Migration 1(nullable) 완료 후 NOT NULL 강제(Migration 2) 전에 반드시 실행된다. 기본 설정값(TENANT-05)도 이 단계에서 seed된다.

Purpose: 기존 데이터를 데이터 손실 없이 멀티테넌트 구조로 전환한다.
Output: 기본 조직 생성, 기존 모든 데이터에 organizationId 채움, ADMIN 계정 생성, 기본 Setting rows 생성
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-multi-tenant-foundation/01-CONTEXT.md
@.planning/phases/01-auth-multi-tenant-foundation/01-RESEARCH.md
@.planning/phases/01-auth-multi-tenant-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01 이후 사용 가능한 Prisma 모델 -->
prisma.organization.create({ data: { id, name, slug } })
prisma.user.create({ data: { organizationId, username, passwordHash, name, role } })
prisma.setting.create({ data: { organizationId, key, value } })
-- backfill 대상 모델: customer, collateral, mortgage, loan, loanSchedule, payment, setting
-- 모두 organizationId가 nullable String?로 존재
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 기본 조직 seed + ADMIN 계정 생성 + 기본 Setting rows (prisma/seed.ts)</name>
  <read_first>
    - prisma/schema.prisma (Organization, User, Setting 모델 필드 확인)
    - package.json (prisma.seed 설정 여부 확인)
  </read_first>
  <files>prisma/seed.ts</files>
  <action>
`prisma/seed.ts` 파일을 생성한다 (없으면 생성, 있으면 내용 추가/교체):

```typescript
import { PrismaClient } from "@prisma/client";
import bcrypt from "bcryptjs";

const prisma = new PrismaClient();

const DEFAULT_ORG_ID = "default-org-001";
const DEFAULT_ORG_SLUG = "default";

async function main() {
  // 1. 기본 조직 upsert
  const org = await prisma.organization.upsert({
    where: { slug: DEFAULT_ORG_SLUG },
    update: {},
    create: {
      id: DEFAULT_ORG_ID,
      name: "기본 조직",
      slug: DEFAULT_ORG_SLUG,
    },
  });
  console.log(`조직 생성/확인: ${org.name} (${org.id})`);

  // 2. ADMIN 계정 upsert (username: admin, 초기 비밀번호: Admin1234)
  const passwordHash = await bcrypt.hash("Admin1234", 12);
  const admin = await prisma.user.upsert({
    where: { username: "admin" },
    update: {},
    create: {
      organizationId: DEFAULT_ORG_ID,
      username: "admin",
      passwordHash,
      name: "관리자",
      role: "ADMIN",
      isActive: true,
    },
  });
  console.log(`관리자 계정 생성/확인: ${admin.username} (role: ${admin.role})`);

  // 3. 기본 Setting rows (TENANT-05: 조직별 기본 설정값 seed)
  // Setting의 unique constraint: @@unique([organizationId, key])
  const defaultSettings = [
    { key: "maxLtvRate", value: "70" },          // 최대 LTV 비율 (%)
    { key: "defaultInterestRate", value: "5.5" }, // 기본 이자율 (%)
    { key: "legalMaxRate", value: "20" },         // 법정 최고 이자율 (%)
    { key: "overdueRate", value: "15" },          // 연체 이자율 (%)
  ];

  for (const setting of defaultSettings) {
    await prisma.setting.upsert({
      where: {
        organizationId_key: {
          organizationId: DEFAULT_ORG_ID,
          key: setting.key,
        },
      },
      update: {},
      create: {
        organizationId: DEFAULT_ORG_ID,
        key: setting.key,
        value: setting.value,
      },
    });
    console.log(`설정 생성/확인: ${setting.key} = ${setting.value}`);
  }
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

`package.json`의 `"prisma"` 키에 seed 설정을 추가한다 (없으면):
```json
"prisma": {
  "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
}
```

ts-node가 없으면 `npm install -D ts-node` 를 실행한다.

seed 실행:
```bash
npx prisma db seed
```
  </action>
  <verify>
    <!-- Phase 1에는 테스트 프레임워크가 없으므로 tsc + 수동 DB 확인으로 검증한다. -->
    <automated>npx prisma db seed 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db seed` 가 오류 없이 완료되고 "조직 생성/확인: 기본 조직" 로그가 출력된다
    - "관리자 계정 생성/확인: admin" 로그가 출력된다
    - "설정 생성/확인: maxLtvRate", "설정 생성/확인: defaultInterestRate", "설정 생성/확인: legalMaxRate", "설정 생성/확인: overdueRate" 로그가 출력된다 (4개 기본 설정)
    - `npx prisma db execute --stdin <<< "SELECT id, name, slug FROM organizations LIMIT 5;"` 가 "default-org-001" 행을 반환한다
    - `npx prisma db execute --stdin <<< "SELECT username, role FROM users WHERE username='admin';"` 가 "admin | ADMIN" 행을 반환한다
    - `npx prisma db execute --stdin <<< "SELECT key, value FROM settings WHERE organization_id='default-org-001';"` 가 4개 설정 행을 반환한다
  </acceptance_criteria>
  <done>기본 조직(id: default-org-001, slug: default), ADMIN 계정(username: admin), 기본 4개 설정(maxLtvRate, defaultInterestRate, legalMaxRate, overdueRate)이 DB에 생성된다</done>
</task>

<task type="auto">
  <name>Task 2: 기존 데이터 backfill (7개 테이블에 organizationId 채우기) + Migration 2 (NOT NULL 강제)</name>
  <read_first>
    - prisma/schema.prisma (7개 기존 모델 이름과 organizationId 필드 확인)
    - .planning/phases/01-auth-multi-tenant-foundation/01-RESEARCH.md (Pattern 4: 3-Step Migration 참조)
  </read_first>
  <files>
    prisma/migrations/backfill-org/run.ts,
    prisma/schema.prisma
  </files>
  <action>
**Step A: backfill 스크립트 작성**

`prisma/migrations/backfill-org/run.ts` 파일을 생성한다:

```typescript
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();
const DEFAULT_ORG_ID = "default-org-001";

async function backfill() {
  const models = [
    "customer",
    "collateral",
    "mortgage",
    "loan",
    "loanSchedule",
    "payment",
    "setting",
  ] as const;

  for (const model of models) {
    const result = await (prisma as any)[model].updateMany({
      where: { organizationId: null },
      data: { organizationId: DEFAULT_ORG_ID },
    });
    console.log(`${model}: ${result.count}개 업데이트`);
  }

  // 검증: null이 남아있는지 확인
  for (const model of models) {
    const nullCount = await (prisma as any)[model].count({
      where: { organizationId: null },
    });
    if (nullCount > 0) {
      throw new Error(`${model}에 organizationId가 null인 행이 ${nullCount}개 남아있습니다`);
    }
  }
  console.log("Backfill 완료: 모든 테이블에 organizationId 설정됨");
}

backfill()
  .catch((e) => {
    console.error("Backfill 실패:", e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

**Step B: backfill 실행**
```bash
npx ts-node --compiler-options '{"module":"CommonJS"}' prisma/migrations/backfill-org/run.ts
```

출력에서 각 모델의 업데이트 수와 "Backfill 완료" 메시지를 확인한다.

**Step C: schema.prisma의 organizationId를 NOT NULL로 변경**

7개 기존 모델의 `organizationId String?` → `organizationId String` 으로 변경한다 (? 제거).
`organization Organization? @relation(...)` → `organization Organization @relation(...)` 으로 변경한다 (? 제거).

변경 대상 모델: Customer, Collateral, Mortgage, Loan, LoanSchedule, Payment, Setting

**Step D: Migration 2 실행**
```bash
npx prisma migrate dev --name set-organizationid-not-null
npx prisma generate
```
  </action>
  <verify>
    <!-- Phase 1에는 테스트 프레임워크가 없으므로 tsc + grep으로 검증한다. 런타임 동작은 수동 확인 필요. -->
    <automated>npx tsc --noEmit 2>&1 | head -10 && grep -c "organizationId String$" /Users/toolb/develop/tool/loan/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - backfill 스크립트가 "Backfill 완료" 메시지를 출력하고 종료 코드 0으로 완료된다
    - `grep -c "organizationId String$" prisma/schema.prisma` 가 7을 반환한다 (7개 모두 NOT NULL)
    - `grep "organizationId String?" prisma/schema.prisma` 가 아무것도 반환하지 않는다 (nullable 없음)
    - `ls prisma/migrations/ | grep set-organizationid-not-null` 가 마이그레이션 디렉토리를 반환한다
    - `npx tsc --noEmit` 가 오류 없이 완료된다
  </acceptance_criteria>
  <done>3단계 마이그레이션 완료: 기존 모든 데이터에 organizationId 채워짐, 7개 테이블의 organizationId가 NOT NULL로 강제됨</done>
</task>

</tasks>

<verification>
1. backfill 스크립트 실행 결과에서 각 모델 업데이트 수 확인
2. `grep -c "organizationId String$" prisma/schema.prisma` → 7 반환
3. `ls prisma/migrations/ | grep set-organizationid-not-null` → 마이그레이션 존재
4. `npx tsc --noEmit` → 오류 없음
5. 수동 확인: DB에 settings 테이블에 4개 기본값(maxLtvRate, defaultInterestRate, legalMaxRate, overdueRate) 존재
</verification>

<success_criteria>
- 3단계 마이그레이션 완료 (Migration 1: nullable → Backfill → Migration 2: NOT NULL)
- 기존 7개 테이블의 모든 row에 organizationId = "default-org-001" 설정
- 기본 조직 및 ADMIN 계정 DB에 존재
- 기본 Setting 4개(maxLtvRate, defaultInterestRate, legalMaxRate, overdueRate) DB에 존재 (TENANT-05)
- TypeScript 컴파일 통과
</success_criteria>

<output>
완료 후 `.planning/phases/01-auth-multi-tenant-foundation/01-02-SUMMARY.md` 를 생성한다.
</output>
