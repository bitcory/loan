---
phase: 01-auth-multi-tenant-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/lib/prisma.ts
  - src/lib/auth.ts
  - src/lib/safe-action.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/middleware.ts
  - src/lib/customer-number.ts
  - .env
  - .env.example
autonomous: true
requirements:
  - TENANT-02
  - TENANT-03
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-06
  - AUTH-07

must_haves:
  truths:
    - "getTenantClient(organizationId) 호출 시 모든 쿼리에 organizationId가 자동 주입된다"
    - "authenticatedAction 미들웨어가 세션 없으면 '인증이 필요합니다' 오류를 던진다"
    - "adminAction 미들웨어가 STAFF role이면 '관리자 권한이 필요합니다' 오류를 던진다"
    - "NextAuth CredentialsProvider가 username/password로 인증하고 JWT에 userId/organizationId/role을 포함한다"
    - "middleware.ts가 /login과 /api/auth 제외한 모든 경로를 보호한다"
    - "User 모델은 Extension의 $allModels 인터셉터에서 제외된다 (auth 시 organizationId 없음)"
  artifacts:
    - path: "src/lib/prisma.ts"
      provides: "getTenantClient 팩토리 + basePrisma export"
      exports: ["getTenantClient", "prisma"]
    - path: "src/lib/auth.ts"
      provides: "NextAuth authOptions (CredentialsProvider + JWT callbacks)"
      exports: ["authOptions"]
    - path: "src/lib/safe-action.ts"
      provides: "actionClient, authenticatedAction, adminAction"
      exports: ["actionClient", "authenticatedAction", "adminAction"]
    - path: "src/middleware.ts"
      provides: "NextAuth 기반 라우트 보호"
      contains: "next-auth/middleware"
  key_links:
    - from: "src/lib/safe-action.ts"
      to: "src/lib/prisma.ts"
      via: "getTenantClient(session.user.organizationId)"
      pattern: "getTenantClient"
    - from: "src/lib/auth.ts"
      to: "prisma.user.findFirst"
      via: "basePrisma (NOT tenant-scoped)"
      pattern: "basePrisma"

user_setup:
  - service: nextauth
    why: "세션 암호화 필수"
    env_vars:
      - name: NEXTAUTH_SECRET
        source: "터미널에서 생성: openssl rand -base64 32"
      - name: NEXTAUTH_URL
        source: "개발: http://localhost:3000"
---

<objective>
Prisma Client Extension으로 테넌트 격리를 구현하고, NextAuth.js v4 CredentialsProvider 인증을 설정하며, next-safe-action 미들웨어 계층(authenticatedAction, adminAction)을 구성한다.

Purpose: 테넌트 격리와 인증의 핵심 인프라. 이후 모든 Server Action과 데이터 접근의 기반.
Output: getTenantClient, authOptions, authenticatedAction, adminAction, middleware.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-multi-tenant-foundation/01-RESEARCH.md
@.planning/phases/01-auth-multi-tenant-foundation/01-01-SUMMARY.md
@.planning/phases/01-auth-multi-tenant-foundation/01-02-SUMMARY.md

<interfaces>
<!-- 기존 src/lib/prisma.ts -->
export const prisma = globalForPrisma.prisma ?? createPrismaClient();
// PrismaPg adapter 사용 중

<!-- 01-01에서 추가된 타입 -->
// src/types/next-auth.d.ts
Session.user: { userId, organizationId, role: "ADMIN" | "STAFF", name? }
JWT: { userId, organizationId, role }

<!-- User 모델 필드 (prisma/schema.prisma) -->
User { id, organizationId, username, passwordHash, name, role, isActive }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 패키지 설치 + 환경변수 + Prisma Client Extension (getTenantClient)</name>
  <read_first>
    - src/lib/prisma.ts (현재 Prisma 싱글톤 구현)
    - package.json (현재 의존성 목록)
    - .env (기존 환경변수 목록 — NEXTAUTH_SECRET 추가 여부 확인)
  </read_first>
  <files>
    src/lib/prisma.ts,
    .env,
    .env.example
  </files>
  <action>
**Step A: 패키지 설치**
```bash
npm install next-auth@4.24.13 "@auth/prisma-adapter@^2.11.1" bcryptjs next-safe-action
npm install -D @types/bcryptjs
```

**Step B: .env에 환경변수 추가**
```
NEXTAUTH_SECRET=<openssl rand -base64 32 실행 결과>
NEXTAUTH_URL=http://localhost:3000
```
실제 SECRET 값을 터미널에서 `openssl rand -base64 32` 로 생성하여 채운다.

**.env.example에도 추가** (값은 placeholder):
```
NEXTAUTH_SECRET=your-secret-here-run-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000
```

**Step C: src/lib/prisma.ts 수정**

파일 전체를 다음으로 교체한다:

```typescript
import { PrismaClient } from "@prisma/client";
import { PrismaPg } from "@prisma/adapter-pg";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

function createPrismaClient() {
  const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL! });
  return new PrismaClient({ adapter });
}

const basePrisma = globalForPrisma.prisma ?? createPrismaClient();

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = basePrisma;

// 테넌트 격리된 클라이언트 팩토리 — 인증된 요청마다 호출
// 중요: User 모델은 인터셉터에서 제외 (auth 시점에 organizationId 컨텍스트 없음)
export function getTenantClient(organizationId: string) {
  return basePrisma.$extends({
    query: {
      customer: {
        async findMany({ args, query }) {
          args.where = { ...args.where, organizationId };
          return query(args);
        },
        async findFirst({ args, query }) {
          args.where = { ...args.where, organizationId };
          return query(args);
        },
        async count({ args, query }) {
          args.where = { ...args.where, organizationId };
          return query(args);
        },
        async create({ args, query }) {
          (args.data as Record<string, unknown>).organizationId = organizationId;
          return query(args);
        },
        async update({ args, query }) {
          args.where = { ...args.where, organizationId };
          return query(args);
        },
        async delete({ args, query }) {
          args.where = { ...args.where, organizationId };
          return query(args);
        },
      },
      collateral: {
        async findMany({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async findFirst({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async count({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async create({ args, query }) { (args.data as Record<string, unknown>).organizationId = organizationId; return query(args); },
        async update({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async delete({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
      },
      mortgage: {
        async findMany({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async findFirst({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async count({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async create({ args, query }) { (args.data as Record<string, unknown>).organizationId = organizationId; return query(args); },
        async update({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async delete({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
      },
      loan: {
        async findMany({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async findFirst({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async count({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async create({ args, query }) { (args.data as Record<string, unknown>).organizationId = organizationId; return query(args); },
        async update({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async delete({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
      },
      loanSchedule: {
        async findMany({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async findFirst({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async count({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async create({ args, query }) { (args.data as Record<string, unknown>).organizationId = organizationId; return query(args); },
        async update({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async delete({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
      },
      payment: {
        async findMany({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async findFirst({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async count({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async create({ args, query }) { (args.data as Record<string, unknown>).organizationId = organizationId; return query(args); },
        async update({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async delete({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
      },
      setting: {
        async findMany({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async findFirst({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async count({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async create({ args, query }) { (args.data as Record<string, unknown>).organizationId = organizationId; return query(args); },
        async update({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
        async delete({ args, query }) { args.where = { ...args.where, organizationId }; return query(args); },
      },
    },
  });
}

// basePrisma: User 모델 등 auth 조회용 (테넌트 필터 없음)
export { basePrisma as prisma };
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function getTenantClient" src/lib/prisma.ts` 가 일치를 반환한다
    - `grep "export { basePrisma as prisma }" src/lib/prisma.ts` 가 일치를 반환한다
    - `grep "User" src/lib/prisma.ts` 가 인터셉터 내부에 User 모델 관련 코드를 반환하지 않는다 (User는 제외됨)
    - `grep "NEXTAUTH_SECRET" .env` 가 값이 있는 줄을 반환한다
    - `grep "NEXTAUTH_URL" .env` 가 일치를 반환한다
    - `npx tsc --noEmit` 가 오류 없이 완료된다
  </acceptance_criteria>
  <done>getTenantClient 팩토리가 7개 비즈니스 모델에 organizationId를 자동 주입하고, User는 제외되어 auth 조회 시 안전하다</done>
</task>

<task type="auto">
  <name>Task 2: NextAuth authOptions + next-safe-action 미들웨어 + middleware.ts + API route</name>
  <read_first>
    - src/lib/prisma.ts (getTenantClient, prisma export 확인 — Task 1 완료 후)
    - src/types/next-auth.d.ts (Session/JWT 타입 확인)
    - .planning/phases/01-auth-multi-tenant-foundation/01-RESEARCH.md (Pattern 2, 3, middleware 코드 참조)
  </read_first>
  <files>
    src/lib/auth.ts,
    src/lib/safe-action.ts,
    src/app/api/auth/[...nextauth]/route.ts,
    src/middleware.ts,
    src/lib/customer-number.ts
  </files>
  <action>
**Step A: src/lib/auth.ts 생성**

```typescript
import { NextAuthOptions } from "next-auth";
import CredentialsProvider from "next-auth/providers/credentials";
import { prisma } from "@/lib/prisma"; // basePrisma — 테넌트 필터 없음
import bcrypt from "bcryptjs";

export const authOptions: NextAuthOptions = {
  session: { strategy: "jwt" },
  pages: { signIn: "/login" },
  providers: [
    CredentialsProvider({
      name: "credentials",
      credentials: {
        username: { label: "아이디", type: "text" },
        password: { label: "비밀번호", type: "password" },
      },
      async authorize(credentials) {
        if (!credentials?.username || !credentials?.password) return null;

        // basePrisma 사용: auth 시점에는 organizationId 컨텍스트 없음
        const user = await prisma.user.findFirst({
          where: { username: credentials.username, isActive: true },
        });

        if (!user) return null;

        const valid = await bcrypt.compare(credentials.password, user.passwordHash);
        if (!valid) return null;

        return {
          id: user.id,
          name: user.name,
          organizationId: user.organizationId,
          role: user.role,
        } as { id: string; name: string; organizationId: string; role: string };
      },
    }),
  ],
  callbacks: {
    async jwt({ token, user }) {
      if (user) {
        token.userId = user.id;
        token.organizationId = (user as { organizationId: string }).organizationId;
        token.role = (user as { role: string }).role;
      }
      return token;
    },
    async session({ session, token }) {
      session.user.userId = token.userId as string;
      session.user.organizationId = token.organizationId as string;
      session.user.role = token.role as "ADMIN" | "STAFF";
      return session;
    },
  },
};
```

**Step B: src/lib/safe-action.ts 생성**

```typescript
import { createSafeActionClient } from "next-safe-action";
import { getServerSession } from "next-auth";
import { authOptions } from "@/lib/auth";
import { getTenantClient } from "@/lib/prisma";

// 비인증 클라이언트 (로그인 액션 전용)
export const actionClient = createSafeActionClient();

// 인증된 클라이언트 — 모든 일반 사용자
export const authenticatedAction = createSafeActionClient().use(
  async ({ next }) => {
    const session = await getServerSession(authOptions);
    if (!session?.user?.userId) {
      throw new Error("인증이 필요합니다.");
    }
    const db = getTenantClient(session.user.organizationId);
    return next({
      ctx: {
        userId: session.user.userId,
        organizationId: session.user.organizationId,
        role: session.user.role,
        db,
      },
    });
  }
);

// 관리자 전용 클라이언트
export const adminAction = createSafeActionClient().use(
  async ({ next }) => {
    const session = await getServerSession(authOptions);
    if (!session?.user?.userId) {
      throw new Error("인증이 필요합니다.");
    }
    if (session.user.role !== "ADMIN") {
      throw new Error("관리자 권한이 필요합니다.");
    }
    const db = getTenantClient(session.user.organizationId);
    return next({
      ctx: {
        userId: session.user.userId,
        organizationId: session.user.organizationId,
        role: "ADMIN" as const,
        db,
      },
    });
  }
);
```

**Step C: src/app/api/auth/[...nextauth]/route.ts 생성**

디렉토리가 없으면 생성:
```bash
mkdir -p src/app/api/auth/\[...nextauth\]
```

파일 내용:
```typescript
import NextAuth from "next-auth";
import { authOptions } from "@/lib/auth";

const handler = NextAuth(authOptions);
export { handler as GET, handler as POST };
```

**Step D: src/middleware.ts 생성**

```typescript
export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    "/((?!api/auth|login|_next/static|_next/image|favicon.ico).*)",
  ],
};
```

**Step E: src/lib/customer-number.ts 수정**

현재 파일을 읽어 `generateCustomerNumber` 함수가 `prisma.customer.findFirst(...)` 패턴을 사용하는지 확인한다. 함수 시그니처를 다음으로 업데이트한다:

```typescript
import { getTenantClient } from "@/lib/prisma";

type TenantDb = ReturnType<typeof getTenantClient>;

export async function generateCustomerNumber(db: TenantDb): Promise<string> {
  const last = await db.customer.findFirst({
    orderBy: { customerNumber: "desc" },
    select: { customerNumber: true },
  });
  const next = last
    ? parseInt(last.customerNumber.replace("C-", ""), 10) + 1
    : 1;
  return `C-${String(next).padStart(4, "0")}`;
}
```

기존에 `prisma`를 import해서 직접 사용하던 코드를 `db` 파라미터로 교체한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const authOptions" src/lib/auth.ts` 가 일치를 반환한다
    - `grep "strategy.*jwt" src/lib/auth.ts` 가 일치를 반환한다
    - `grep "아이디 또는 비밀번호가 올바르지 않습니다" src/lib/auth.ts` 가 **없음** — authorize는 null 반환, 에러 메시지는 로그인 UI에서 처리 (보안상 서버에서 구체적 이유 노출 금지)
    - `grep "export const authenticatedAction" src/lib/safe-action.ts` 가 일치를 반환한다
    - `grep "export const adminAction" src/lib/safe-action.ts` 가 일치를 반환한다
    - `grep "관리자 권한이 필요합니다" src/lib/safe-action.ts` 가 일치를 반환한다
    - `ls src/app/api/auth/\[...nextauth\]/route.ts` 가 파일을 반환한다
    - `grep "next-auth/middleware" src/middleware.ts` 가 일치를 반환한다
    - `grep "TenantDb" src/lib/customer-number.ts` 가 일치를 반환한다
    - `npx tsc --noEmit` 가 오류 없이 완료된다
  </acceptance_criteria>
  <done>NextAuth JWT 인증, next-safe-action 미들웨어 계층(인증/관리자), 라우트 보호 middleware.ts가 구성되어 모든 Server Action이 인증을 검증할 준비가 된다</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — 오류 없음
2. `grep "export function getTenantClient" src/lib/prisma.ts` — 존재
3. `grep "export const adminAction" src/lib/safe-action.ts` — 존재
4. `ls src/app/api/auth/\[...nextauth\]/route.ts` — 파일 존재
5. `grep "NEXTAUTH_SECRET" .env` — 값 있음
</verification>

<success_criteria>
- getTenantClient가 7개 비즈니스 모델에 organizationId 자동 주입 (User 제외)
- NextAuth CredentialsProvider + JWT 세션 전략 구성
- authenticatedAction, adminAction 미들웨어 사용 가능
- middleware.ts로 /login, /api/auth 외 모든 라우트 보호
- npx tsc --noEmit 통과
</success_criteria>

<output>
완료 후 `.planning/phases/01-auth-multi-tenant-foundation/01-03-SUMMARY.md` 를 생성한다.
</output>
